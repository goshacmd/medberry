db:
  image: orchardup/postgresql
  volumes:
    - tmp/db:/var/lib/postgresql
  ports:
    - 5432

web:
  build: .
  command: bundle exec rackup -p 3000
  volumes:
    - .:/home/app
  ports:
    - 3000:3000
  links:
    - db
