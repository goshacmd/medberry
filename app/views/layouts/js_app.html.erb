<!DOCTYPE html>
<html>
<head>
  <title>medberry</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>

<header>
  <nav class="navbar navbar-default" role="navigation">
    <div class="container">
      <%= render 'layouts/navigation' %>
    </div>
  </nav>
</header>

<div id="medberry-app">
</div>

<%= render 'layouts/footer' %>

<script src="https://swww.tokbox.com/webrtc/v2.0/js/TB.min.js"></script>
<script src="https://d3dy5gmtp8yhk7.cloudfront.net/2.2/pusher.min.js"></script>

<%= javascript_include_tag 'application' %>
<%= javascript_include_tag 'translations' %>

<script type="text/javascript">
  I18n.defaultLocale = "<%= I18n.default_locale %>";
  I18n.locale = "<%= I18n.locale %>";
  I18n.fallbacks = true;

  var currentUser = JSON.parse('<%= UserSerializer.new(current_user).to_json.html_safe %>');
  var tokboxApiKey = "<%= ENV['OPEN_TOK_KEY'] %>";
  var pusherKey = "<%= Pusher.key %>";
  var userChannelName = "<%= current_user.pusher_channel_name %>";
  var pulserChannelName = "<%= current_user.pulser_channel_name %>";
</script>

</body>
</html>
