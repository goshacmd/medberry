<h2>
  Insurance companies
  <small><%= @companies.count %></small>

  <span class="pull-right">
    <%= link_to 'Add', new_admin_insurance_company_path, class: 'btn btn-success' %>
  </span>
</h2>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th># users</th>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %></td>
        <td><%= company.insurance_policies.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
