{{#unless cameraAccessError}}
  <div class="videos">
    <div id="video-self"></div>
    <div id="video-mate"></div>
  </div>
{{else}}
  <h3>{{t 'camera.denied_header'}}</h3>

  <p>{{t 'camera.denied_body'}}</p>
{{/unless}}
